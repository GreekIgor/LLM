"""
Маленькая база знаний для RAG-приложения (ДЗ-19).

Тема — язык Python и его экосистема: факты проверяемые и однозначные, что удобно
для оценки галлюцинаций (Faithfulness) и полноты контекста (Context Recall).
Каждый документ — короткий самодостаточный абзац с уникальным id.
"""

CORPUS: list[dict] = [
    {
        "id": "history",
        "text": (
            "Язык программирования Python создал Гвидо ван Россум. Первый публичный "
            "релиз Python 0.9.0 состоялся в феврале 1991 года. Название языка отсылает "
            "не к змее, а к британскому комедийному шоу 'Monty Python's Flying Circus'."
        ),
    },
    {
        "id": "typing",
        "text": (
            "Python — язык с динамической строгой типизацией: типы проверяются во время "
            "выполнения, а не на этапе компиляции. Аннотации типов (PEP 484) опциональны "
            "и по умолчанию не влияют на выполнение, их проверяют внешние инструменты вроде mypy."
        ),
    },
    {
        "id": "pep8",
        "text": (
            "PEP 8 — это руководство по стилю оформления кода на Python. Оно рекомендует "
            "отступ в 4 пробела, ограничение длины строки 79 символов и стиль snake_case "
            "для имён функций и переменных."
        ),
    },
    {
        "id": "gil",
        "text": (
            "GIL (Global Interpreter Lock) — это мьютекс в CPython, который позволяет "
            "исполнять байт-код Python только одному потоку одновременно. Из-за GIL потоки "
            "не дают ускорения на CPU-bound задачах, поэтому для параллелизма по CPU "
            "используют процессы (модуль multiprocessing)."
        ),
    },
    {
        "id": "pip",
        "text": (
            "pip — стандартный менеджер пакетов Python. Он устанавливает пакеты из "
            "репозитория PyPI (Python Package Index). Команда установки выглядит как "
            "'pip install <имя_пакета>', а список зависимостей принято хранить в requirements.txt."
        ),
    },
    {
        "id": "venv",
        "text": (
            "Виртуальное окружение изолирует зависимости проекта от системного Python. "
            "В стандартной библиотеке за это отвечает модуль venv, окружение создаётся "
            "командой 'python -m venv .venv'. Это позволяет разным проектам использовать "
            "разные версии одних и тех же пакетов."
        ),
    },
    {
        "id": "datastructures",
        "text": (
            "Базовые встроенные коллекции Python: list (изменяемый упорядоченный список), "
            "tuple (неизменяемый кортеж), dict (словарь пар ключ-значение) и set "
            "(множество уникальных элементов). Списки и словари изменяемы, а кортежи — нет."
        ),
    },
    {
        "id": "comprehensions",
        "text": (
            "List comprehension — компактный синтаксис создания списка из итерируемого "
            "объекта, например [x*x for x in range(10) if x % 2 == 0]. Аналогично существуют "
            "генераторы множеств и словарей, а также ленивые генераторные выражения в круглых скобках."
        ),
    },
    {
        "id": "cpython",
        "text": (
            "CPython — это эталонная и самая распространённая реализация интерпретатора "
            "Python, написанная на C. Существуют и альтернативные реализации: PyPy с "
            "JIT-компиляцией, Jython для JVM и IronPython для .NET."
        ),
    },
    {
        "id": "asyncio",
        "text": (
            "asyncio — модуль стандартной библиотеки для асинхронного программирования на "
            "основе сопрограмм. Сопрограммы объявляются через 'async def', а ожидание "
            "результата — через 'await'. Это эффективно для I/O-bound задач, но не обходит GIL для CPU."
        ),
    },
    {
        "id": "zen",
        "text": (
            "PEP 20, известный как 'The Zen of Python', формулирует принципы дизайна языка. "
            "Среди них: 'Явное лучше неявного', 'Простое лучше сложного', 'Читаемость "
            "важна'. Текст можно увидеть, выполнив 'import this'."
        ),
    },
    {
        "id": "py2eol",
        "text": (
            "Поддержка Python 2 официально прекращена 1 января 2020 года. Python 3, "
            "вышедший в 2008 году, не имеет полной обратной совместимости с Python 2: "
            "например, print стал функцией, а строки по умолчанию хранятся в Unicode."
        ),
    },
    {
        "id": "numpy",
        "text": (
            "NumPy — библиотека для научных вычислений, ядро которой — многомерный массив "
            "ndarray. Операции над массивами векторизованы и выполняются в скомпилированном "
            "коде на C, что значительно быстрее обычных циклов Python."
        ),
    },
    {
        "id": "pandas",
        "text": (
            "pandas — библиотека для анализа табличных данных. Её основные структуры — "
            "Series (одномерный массив с метками) и DataFrame (двумерная таблица). pandas "
            "построена поверх NumPy и удобна для загрузки, очистки и агрегации данных."
        ),
    },
]


def get_corpus() -> list[dict]:
    """Возвращает копию списка документов базы знаний."""
    return [dict(d) for d in CORPUS]
