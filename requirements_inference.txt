# Зависимости только для inference-сервиса в контейнере
# (без vllm/torch/jupyter — они не нужны внутри FastAPI-контейнера)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
prometheus-client>=0.20.0
mlflow>=2.9.0
requests>=2.31.0
