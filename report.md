# Отчёт: оценка качества QA-генерации с помощью метрик (Трек A)

> Экспортировать в PDF для сдачи (`File → Export to PDF` / `pandoc report.md -o report.pdf`).
> Ячейки с `‹…›` заполняются числами из прогона ноутбука (`summary`, `results.csv`).

## 1. Постановка задачи

Сравниваются три бесплатные LLM на задаче Question Answering на русском языке.
Цель — выбрать модель под **фактологический QA** и понять, какие метрики качества
наиболее информативны для этой задачи.

- **Датасет:** `kuznetsoffandrey/sberquad` (русская версия SQuAD), срез 80 примеров
  валидационного сплита, seed=42.
- **Модели (OpenRouter, free):** Llama-3.3-70B, Gemini-2.0-flash-exp, Qwen-2.5-72B.
- **Метрики:** Exact Match, token-F1, BLEU, Semantic Similarity, время генерации, длина.

## 2. Методология

- Ответы запрашиваются в максимально кратком формате (одно слово/фраза), чтобы EM и F1
  не штрафовались за «многословность» — это приближает условия к разметке SQuAD.
- **Нормализация** перед EM/F1 (см. `metrics.py::normalize_text`): нижний регистр,
  удаление пунктуации, выкидывание частиц-филлеров, схлопывание пробелов.
- token-F1 — стандартная SQuAD-метрика (F1 по пересечению мультимножеств токенов).
- Semantic Similarity — косинус эмбеддингов `paraphrase-multilingual-MiniLM-L12-v2`.
- Все модели и форматы прогоняются на **одних и тех же** примерах → корректно для
  парных тестов значимости.

## 3. Сравнительная таблица результатов

*(из `summary.csv`)*

| Модель | EM | F1 | BLEU | SemSim | Латентность, с | Длина ответа |
|--------|----|----|------|--------|----------------|--------------|
| Llama-3.3-70B  | ‹ › | ‹ › | ‹ › | ‹ › | ‹ › | ‹ › |
| Gemini-2.0-flash | ‹ › | ‹ › | ‹ › | ‹ › | ‹ › | ‹ › |
| Qwen-2.5-72B   | ‹ › | ‹ › | ‹ › | ‹ › | ‹ › | ‹ › |

**95% CI для F1 (bootstrap)** и **парный тест значимости** — из раздела 7 ноутбука:
‹вставить вывод: значима ли разница между лучшей и остальными моделями›.

## 4. Эксперименты

### 4.1 Влияние длины/формата промпта (short vs detailed)
‹Тезис: подробная инструкция с требованием краткости обычно повышает EM/F1, т.к.
стабилизирует формат короткого ответа и убирает «рассуждения вслух».›

### 4.2 Zero-shot vs few-shot
‹Тезис: few-shot (2 примера) даёт прирост прежде всего по EM — модель копирует формат
эталона; на SemSim эффект слабее, т.к. смысл и так улавливается.›

### 4.3 Влияние температуры (0.0 / 0.3 / 0.7)
‹Тезис: для QA оптимум около t=0.0; рост температуры увеличивает длину ответа и
снижает EM из-за вариативности формулировок.›

## 5. Качественный анализ примеров (5–10 шт.)

*(из раздела 6.3 ноутбука)*

**Худшие (низкий F1):**
1. ‹вопрос → эталон / предсказание — комментарий, тип ошибки›
2. …

**Лучшие (F1 ≈ 1):**
1. ‹вопрос → эталон / предсказание›
2. …

**Типичные ошибки:**
- *Форма ≠ смысл:* модель отвечает верно по сути, но другой падеж/синоним → EM=0 при
  высоком SemSim (доля таких случаев — ‹X%›).
- *Многословность:* развёрнутый ответ вместо короткого → падает F1.
- *Галлюцинация/выход за контекст:* ответ не из текста.

## 6. Критический анализ метрик

| Метрика | Информативность | Комментарий |
|---------|-----------------|-------------|
| **F1 (token)** | Высокая | Лучший компромисс: учитывает частичное совпадение, устойчива к одному лишнему слову. |
| **Semantic Similarity** | Высокая | Ловит правильные по смыслу, но иначе сформулированные ответы — то, что EM теряет. |
| **Exact Match** | Низкая–средняя | Сильно занижена на русском: падежи, синонимы, пунктуация. Полезна как «нижняя граница строгости». |
| **BLEU** | Низкая | Плохо работает на коротких ответах (n-граммы почти не совпадают); информативна только в относительном сравнении. |
| **Латентность / длина** | Контекстная | Важны для прод-выбора, не для качества как такового. |

**Вывод по метрикам:** для фактологического QA на русском связка **F1 + SemSim**
информативнее всего; EM и BLEU стоит приводить, но не делать на них главный вывод.

## 7. Рекомендации по выбору модели

- **Под качество фактологии:** ‹модель с макс. F1/SemSim›.
- **Под скорость/латентность:** ‹модель с мин. латентностью при приемлемом F1›.
- **Промптинг:** формат `detailed` + few-shot, температура 0.0.

## 8. Бонус: статистическая значимость

Bootstrap (10k ресэмплов): 95% CI для F1 каждой модели + парный bootstrap-тест разницы.
Вывод о значимости преимущества лучшей модели — ‹значимо / не значимо при p‹0.05›.
