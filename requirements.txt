# ДЗ-17: Извлечение сущностей и событий (NER + Information Extraction)

# ── Локальный inference (CPU или NVIDIA GPU — выбирается автоматически) ───────
torch>=2.2.0             # для GPU ставь CUDA-сборку (напр. cu124)
transformers>=4.44.0
datasets>=2.20.0          # загрузка CUAD
accelerate>=0.33.0        # device_map, ускорение загрузки

# ── Провайдер через API (OpenRouter / LM Studio, OpenAI-совместимый) ──────────
openai>=1.30.0           # клиент для LLM_PROVIDER=openrouter | lmstudio

# ── Метрики и анализ ─────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.0.0             # таблицы сравнения моделей
psutil>=5.9.0             # замер RAM локально
matplotlib>=3.7.0         # графики throughput / качество (в ноутбуке)

# ── Demo-приложение ──────────────────────────────────────────────────────────
gradio>=4.0.0            # интерактивная демонстрация извлечения

# ── Только для Colab T4 (7B-модели, 4-битное квантование) ────────────────────
# Раскомментируй в Colab; локально на Windows без CUDA bitsandbytes не работает.
# bitsandbytes>=0.43.0   # 4-bit квантование (QLoRA-стиль), нужен CUDA
# sentencepiece>=0.2.0   # токенайзер Llama-2 / Mistral
